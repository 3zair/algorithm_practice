#include <cstdio>
#include <vector>
using namespace std;

int countNegatives(vector<vector<int>> &grid) {
    vector<vector<int>> as;
    return 0;
}
int main() {
    vector<vector<int>> text;
    countNegatives(text);
    return 0;
}